query lookups {
    thingTypes {
        id
        name
        validClasses {
            id
            name
        }
        contentTypeMap
    }

    correspondents {
        nodes {
            id
            name
        }
    }
}
