query lookups {
    thingTypes {
        id
        name
        validClasses {
            id
            name
            properties {
                id
                name
                dataType
            }
        }
        contentTypeMap
    }
}
