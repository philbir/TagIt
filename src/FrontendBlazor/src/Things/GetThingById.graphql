query getThingById($id: ID!) {
  thingById(id: $id){
    ... ThingDetail
  }
}

fragment ThingDetail on Thing {
  id
  title
  type {
    name
  }
  source {
    connectorId
    connectorId
  }
  date
  classId
  state
  thumbnail(loadData: true, pageNumber: 1) {
    url
  }
}
