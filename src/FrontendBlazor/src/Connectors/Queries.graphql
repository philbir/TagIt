query connectorSearch {
  connectors {
    nodes {
      ...ConnectorInfo
    }
  }
}

fragment ConnectorInfo on ConnectorDefintion {
  id
  type
  name
  root
  credential {
    id
    name
  }
}   
